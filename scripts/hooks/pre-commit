#!/usr/bin/env bash
# Pre-commit hook: reject staged skill changes with deterministic authoring defects.

set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
"${REPO_ROOT}/scripts/validate-skill-authoring.sh" --staged
