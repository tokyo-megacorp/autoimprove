{
  "challenge": "goroutine-leak",
  "language": "go",
  "bugs": [
    {
      "id": "B1",
      "file": "challenge.go",
      "line": 40,
      "type": "goroutine-leak",
      "severity": "critical",
      "description": "Unbuffered channel causes goroutine leak. When Search returns early from select, remaining goroutines block forever trying to send on unbuffered channel ch.",
      "fix_pattern": "make(chan Result, len(backends))",
      "fix_pattern_mode": "substring"
    }
  ],
  "scoring": {
    "match_file": true,
    "match_line_range": 5,
    "match_type": true
  }
}
