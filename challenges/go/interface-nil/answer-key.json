{
  "challenge": "interface-nil",
  "language": "go",
  "bugs": [
    {
      "id": "B1",
      "file": "challenge.go",
      "line": 67,
      "type": "interface-nil",
      "severity": "critical",
      "description": "Typed nil returned: NewLogger returns *FileLogger(nil) wrapped in Logger interface, making the interface non-nil even though the underlying pointer is nil. Caller cannot detect this with != nil check.",
      "fix_pattern": "return &NoOpLogger{}, nil",
      "fix_pattern_mode": "substring"
    },
    {
      "id": "B2",
      "file": "challenge.go",
      "line": 84,
      "type": "insufficient-nil-check",
      "severity": "high",
      "description": "Interface nil check insufficient: 'if logger != nil' only checks the interface wrapper, not the underlying pointer. Should use type assertion or always return properly initialized logger.",
      "fix_pattern": "if logger == nil || reflect.ValueOf(logger).IsNil()",
      "fix_pattern_mode": "substring"
    }
  ],
  "scoring": {
    "match_file": true,
    "match_line_range": 5,
    "match_type": true
  }
}
